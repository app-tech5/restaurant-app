id: complex-component
message: "Component has multiple useState hooks. Consider using useReducer or extracting state logic"
language: JavaScript
rule:
  pattern: useState($$$)
  inside:
    kind: function_declaration
    has:
      pattern: useState($$$)
      has:
        pattern: useState($$$)
